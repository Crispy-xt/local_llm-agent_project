The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. The small language model learns the next token. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. TinyLM learns from examples. 